#include <iostream>

using namespace std;

int main(void){
    // create two variable called a and b
    int a;
    int b;

    // get a and b from user
    cin >> a;
    cin >> b;

    int sum = a + b;

    // output the sum of a and b
    cout << sum << endl;

    return 0;
}